# AGENTS.md

本文件用于指导 Codex / AI 编程助手在本项目中的开发行为。核心原则是：**每次只增添、删除或修改一个功能，保持改动小、可审查、可回退，确保项目始终处于用户可控范围内。**

---

## 1. 项目定位

本项目是一个面向食谱质量量化的评分工具，目标是根据食谱中的食材、重量、烹调方式、餐次、用户目标和个人偏好，输出可解释的食谱质量评分。

项目核心不只是页面展示，而是：

1. 将自然语言食谱转化为结构化数据；
2. 根据规则和营养数据库进行可复现计算；
3. 输出评分、扣分原因、优点和优化建议；
4. 保持算法过程透明、可测试、可调整。

---

## 2. 总体开发原则

### 2.1 一次只做一个功能

每次开发任务只能包含以下一种改动类型：

- 新增一个功能；
- 修改一个已有功能；
- 删除一个已有功能；
- 修复一个明确 bug；
- 重构一个明确范围内的模块；
- 添加一组与当前功能直接相关的测试。

禁止在一次任务中同时进行多个无关改动。

例如：

```text
允许：新增“盐分评分”计算函数。
不允许：同时新增盐分评分、修改页面布局、重构数据库结构、调整按钮样式。
```

### 2.2 小步提交

每次改动应尽量小，优先保证：

- 改动范围清晰；
- 文件数量少；
- 功能边界明确；
- 方便人工审查；
- 出问题后容易回退。

如果任务过大，必须先拆分为多个小任务，不要一次性实现完整大模块。

### 2.3 不要擅自扩大范围

Codex 不应主动添加用户没有要求的功能。

禁止行为包括：

- 擅自更换技术栈；
- 擅自引入大型依赖；
- 擅自重构整个项目；
- 擅自改变目录结构；
- 擅自修改评分权重；
- 擅自删除已有逻辑；
- 擅自添加复杂 UI 或动画；
- 擅自把临时实验代码合入主逻辑。

如果发现某处需要额外修改，应在回复中说明建议，而不是直接修改。

---

## 3. 每次任务的固定流程

每次开始编码前，Codex 必须先完成以下步骤。

### 3.1 先理解任务

明确本次任务的唯一目标，例如：

```text
本次任务目标：新增烹调方式评分规则。
```

同时说明不会改动的内容，例如：

```text
本次不修改前端页面，不调整总分权重，不改动数据库结构。
```

### 3.2 先给出修改计划

在实际改代码前，先列出计划：

```text
计划：
1. 在 scoring 模块中新增 cookingMethodScore 函数。
2. 在规则配置中加入烹调方式与分值映射。
3. 添加对应单元测试。
4. 运行测试，确认已有行为不受影响。
```

计划必须具体，不要写成空泛描述。

### 3.3 再执行修改

执行修改时，只能修改与当前任务直接相关的文件。

如果需要新增文件，应说明文件用途。

如果需要修改已有文件，应尽量保持原有结构，不要大范围重写。

### 3.4 修改后必须说明结果

完成后需要说明：

- 修改了哪些文件；
- 新增或修改了什么逻辑；
- 是否运行测试；
- 测试是否通过；
- 有没有尚未解决的问题。

---

## 4. 代码修改边界

### 4.1 前端修改边界

修改前端时，应避免同时修改算法逻辑。

允许：

- 增加一个评分结果展示组件；
- 修改一个按钮样式；
- 增加一个输入表单字段；
- 调整一个页面布局。

不允许：

- 修改页面时顺便改评分公式；
- 改按钮时顺便重构状态管理；
- 加表单时顺便调整数据库结构。

### 4.2 后端修改边界

修改后端时，应避免同时修改前端 UI。

允许：

- 新增一个评分函数；
- 新增一个 API endpoint；
- 修改一个 schema；
- 新增一组测试。

不允许：

- 新增 API 时顺便重构全部路由；
- 修改 schema 时顺便更换数据库；
- 调整评分函数时顺便修改前端页面。

### 4.3 规则文件修改边界

修改规则文件时，应明确说明：

- 修改了哪个阈值；
- 为什么修改；
- 会影响哪个评分项；
- 是否需要同步更新测试。

---

## 5. 测试要求

每次功能改动后，必须尽量运行与本次改动相关的测试。

### 5.1 评分函数必须有测试

评分算法相关函数必须至少覆盖：

- 正常输入；
- 边界输入；
- 缺失输入；
- 极端输入；
- 触发上限规则的输入。

例如盐分评分应测试：

```text
实际盐 <= 上限：满分
实际盐 = 1.5 × 上限：部分扣分
实际盐 >= 2 × 上限：0 分
缺少盐数据：返回默认值或警告
```

### 5.2 不要只测试 happy path

禁止只测试理想情况。

必须考虑：

- 用户没有填写重量；
- 食材无法匹配数据库；
- 烹调方式未知；
- 餐次未知；
- 用户偏好为空；
- 规则配置缺失。

### 5.3 测试失败时不要继续扩展功能

如果测试失败，应优先修复当前问题。

禁止在测试失败的状态下继续新增无关功能。

---

## 6. 数据处理规范

### 6.1 食材名称标准化

用户输入的食材名称应先标准化，再进入计算。

例如：

```text
番茄 / 西红柿 → 番茄
土豆 / 马铃薯 → 马铃薯
米饭 / 熟米饭 → 熟米饭
```

### 6.2 不确定数据要标记

如果系统无法确认某个食材或重量，不要假装准确。

应返回：

```text
数据不完整，以下结果为估算。
```

或在结构化结果中标记：

```ts
confidence: "low"
```

### 6.3 缺失数据不要静默忽略

如果缺少关键数据，应生成 warning。

例如：

```ts
warnings: [
  "未提供食用油用量，油脂控制分使用默认估算值。"
]
```

---

## 7. 禁止事项

Codex 在本项目中禁止：

1. 一次完成多个功能；
2. 未说明计划就大范围修改代码；
3. 擅自修改评分总权重；
4. 擅自删除已有测试；
5. 用 AI 直接生成最终分数替代规则计算；
6. 把评分逻辑写死在 UI 组件中；
7. 引入不必要的大型依赖；
8. 在测试失败时继续开发新功能；
9. 把不确定估算写成确定结论；
10. 未经要求重构整个项目结构。

---

## 8. 推荐回复格式

Codex 每次完成任务后，应按以下格式回复：

```text
本次任务：
- 实现了 XXX。

修改文件：
- path/to/file.ts：新增 XXX 逻辑。
- path/to/test.ts：新增 XXX 测试。

未修改内容：
- 未修改评分总权重。
- 未修改前端页面。
- 未修改数据库结构。

测试结果：
- 已运行 npm test，全部通过。

注意事项：
- XXX 数据当前仍为估算，后续可接入正式营养数据库。
```

---

## 9. 开发优先级建议

建议按以下顺序小步推进：

1. 建立食谱输入结构；
2. 建立食材标准化映射；
3. 建立基础营养数据表；
4. 实现单项评分函数；
5. 实现总分聚合函数；
6. 添加模块化测试；
7. 建立评分结果展示页面；
8. 加入用户偏好输入；
9. 加入个性化接受度评分；
10. 加入 AI 解释生成。

每一步都应独立完成、独立测试，不要合并成一次大改动。

---

## 10. 最重要的约束

任何时候都优先遵守以下规则：

```text
一次只改一个功能。
先说明计划，再改代码。
只改相关文件。
保留可测试性。
保留可回退性。
不要让 AI 直接决定分数。
不要让项目脱离用户控制。
```
